public enum charRole{   // Character roles
    Adventurer,
    Thief,
    Archer,
    Warrior,
    Wizard,
    Priest
}

internal enum roomType{ // Room types
    Empty,
    Altar,
    Puzzle,
    Battle,
    Pool
}

internal enum attribute{    //Attribute names
    STR, 
    DEX, 
    END, 
    WILL, 
    INT, 
    WIS, 
    LUCK
}